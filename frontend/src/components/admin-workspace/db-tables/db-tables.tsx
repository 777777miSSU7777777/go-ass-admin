import React from 'react';
import styles from './db-tables.module.scss';

interface Props {}

const DBTables = (props: Props) => {
    return (
        <div className={styles.dbTables}>DB Tables</div>
    )
}

export default DBTables;