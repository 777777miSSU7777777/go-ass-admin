import React from 'react';
import styles from './admin-workspace.module.scss';

interface Props {}

const AdminWorkspace = (props: Props) => {
    return (
        <div className={styles.adminWorkspace}>Admin Workspace</div>
    )
}

export default AdminWorkspace;