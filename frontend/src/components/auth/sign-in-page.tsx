import React from 'react';
import styles from './sign-in-page.module.scss';

interface Props {}

const SignInPage = (props: Props) => {
    return (
        <div className={styles.signInPage}>Sign In Page</div>
    )
}

export default SignInPage;