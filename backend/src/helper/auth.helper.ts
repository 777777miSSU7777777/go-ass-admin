export enum TokensDuration {
    access = 1800,
    refresh = 518400,
}

export enum TokenTypes {
    access = "access",
    refresh = "refresh",
}

export enum UserRoles {
    user = "user",
    admin = "admin",
}

export const SECRET_KEY = "NOT A SECRET KEY";